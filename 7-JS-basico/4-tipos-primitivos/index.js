// tipo estring - aqui estamos colocado uma string dentro das aspas, e elas podem ser letras ou numeros,
let nome = 'Sany'
let idade = '25'

// o number - ele pode ser um número inteiro, decimais ou negativos, mas não ficam dentro de aspas, nem duplas nem simples;
let altura = 161.5

//o booleano serve para validar se uma condição é verdadeira ou falsa, ele recebe true ou false;
let usuarioVerificado = false
//exe:

if(usuarioVerificado){
   console.log("usuário verificado");
}else{
   console.log("usuário não verificado")
}
//vai aparecer no console usuário não verificado, pois definimos como false na declaração da variável acima.

// a variável null: vai apontar um endereço de memória inválido ou inexistente;
let pessoa = null // não vaiser criado nada nesse espaço da memória.
